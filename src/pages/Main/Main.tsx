import React from 'react';
import {SafeAreaView, View} from 'react-native';
import axios from 'axios';
import {StaticItem, StaticTollBadge} from '@components';

const Main = () => {
  return (
    <SafeAreaView>
      <StaticItem />
    </SafeAreaView>
  );
};

export default Main;
